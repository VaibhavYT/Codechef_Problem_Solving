#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n, x;
        cin >> n >> x;
        if (n == 1)
        {
            cout << x << endl;
            continue;
        }
        if (n > 1)
        {
            while (a)
            {
                /* code */
            }
            
        }
    }
}